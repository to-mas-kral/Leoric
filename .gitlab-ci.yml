stages:
  - build

.rust-latest:
  stage: build
  image: rustdocker/rust:stable
  script:
    - cargo build --verbose
    - cargo test --verbose

build-linux:
  extends: .rust-latest
  tags:
    - linux

build-windows:
  extends: .rust-latest
  tags:
    - windows
